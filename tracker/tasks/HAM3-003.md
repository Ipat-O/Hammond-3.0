# HAM3-003 — Local directory contexts and bindings

State: `testing`
Worker: Anthropic / Claude Code / Claude Sonnet 5
Auditor: Kilo Code / DeepSeek V4 Pro  
Depends on: HAM3-001

Work branch: `anthropic/HAM3-003-local-directory-contexts`
Issue: not available; the repository has no HAM3-003 GitHub issue
Base SHA: `c22efc720ae299337afd0f81635dda602b1e083e`
Current head: `b1362ee44108b68f8160b58eb59f76f3bca8cf4c`
Draft PR: [#6](https://github.com/Ipat-O/Hammond-3.0/pull/6)
Worker report: [PR comment](https://github.com/Ipat-O/Hammond-3.0/pull/6#issuecomment-5297097501)
Audit verdict: DeepSeek V4 Pro `APPROVE` at `b1362ee44108b68f8160b58eb59f76f3bca8cf4c`
Audit report: [PR comment](https://github.com/Ipat-O/Hammond-3.0/pull/6#issuecomment-5297622312)

Audit result: confinement, a real Windows junction escape, picker/reveal wiring, local settings/recovery, unlink-without-delete, Supabase isolation, incidental dependencies/icons, packaging, and mutation proofs all pass. The missing-root `path_exists` ordering is a non-blocking documentation inaccuracy because the production reachability caller passes an empty target and no malformed target is touched.

## Outcome

Let the owner open, change, close, and resume local directories directly through the desktop application.

## Scope

- Native folder picker.
- Typed read/write/remove/existence/reveal commands constrained to the selected directory.
- Local-only project-to-directory bindings.
- Multiple directories linked to one project.
- Last-open directory and screen resume state.
- Missing/moved directory recovery UI.
- Treat ordinary folders, repositories, and Git worktrees uniformly.

## Non-scope

Git validation, branch discovery, remotes, dirty-state inspection, GitHub, or Supabase storage of absolute paths.

## Acceptance

- Picker returns or cancels immediately with no waiting state.
- Open/change/close works without a bridge or localhost service.
- Restart resumes an existing directory or clearly reports that it moved.
- Two worktree paths can link to the same project as separate local contexts.

## Human check

Owner uses the native picker to open a normal folder, a repository, and a worktree; cancels the picker once; links two contexts to one project; switches and reveals them; closes one and confirms nothing is deleted; restarts and confirms resume; then moves or renames a linked directory, confirms missing-directory recovery, and locates its replacement.
